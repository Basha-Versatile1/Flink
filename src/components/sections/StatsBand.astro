---
import Counter from "@/components/ui/Counter.astro";
import Icon from "@/components/Icon.astro";
import Reveal from "@/components/ui/Reveal.astro";
import { STATS } from "@/data/site";
import type { IconName } from "@/lib/icons";

// Numbers are placeholders ([N]+) until the client supplies real figures (§2).
---

<section aria-label="Flink by the numbers" class="border-b border-hairline bg-white">
  <div class="container-px py-14">
    <dl class="grid grid-cols-2 gap-4 lg:grid-cols-4">
      {
        STATS.map((stat, i) => (
          <Reveal as="div" delay={(i % 4) * 70}>
            <div class="flex h-full items-center gap-4 rounded-card border border-primary/10 bg-gradient-to-b from-white to-surface-cyan/70 p-5 shadow-[inset_0_1px_0_0_rgba(255,255,255,0.9),0_10px_24px_-16px_rgba(6,182,212,0.35)]">
              <span class="inline-flex h-12 w-12 shrink-0 items-center justify-center rounded-2xl bg-gradient-to-b from-white to-slate-50 text-primary-deep ring-1 ring-primary/15 shadow-[inset_0_1px_0_0_rgba(255,255,255,0.9),0_4px_10px_-4px_rgba(6,182,212,0.25)]">
                <Icon name={stat.icon as IconName} size={24} />
              </span>
              <div>
                <dd class="text-2xl font-bold tracking-tight text-ink sm:text-3xl">
                  <Counter value={stat.value} display={stat.display} suffix={stat.suffix} />
                </dd>
                <dt class="mt-0.5 text-sm font-medium text-ink-soft">{stat.label}</dt>
              </div>
            </div>
          </Reveal>
        ))
      }
    </dl>
  </div>
</section>
