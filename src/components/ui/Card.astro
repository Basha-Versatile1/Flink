---
import { cn } from "@/lib/cn";

interface Props {
  class?: string;
  hover?: boolean;
  as?: "div" | "article" | "li";
}

const { class: className, hover = false, as: Tag = "div" } = Astro.props;
---

<Tag
  class={cn(
    // Glossy: subtle top-down sheen gradient + inset white highlight.
    "rounded-card bg-gradient-to-b from-white to-slate-50/80 p-6 ring-1 ring-hairline/70 shadow-[0_1px_2px_rgba(15,23,42,0.04),inset_0_1px_0_0_rgba(255,255,255,0.9)]",
    hover &&
      "transition-all duration-200 hover:-translate-y-0.5 hover:ring-primary/40 hover:shadow-[0_18px_40px_-16px_rgba(15,23,42,0.22),inset_0_1px_0_0_rgba(255,255,255,1)]",
    className,
  )}
>
  <slot />
</Tag>
