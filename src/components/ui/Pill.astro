---
import { cn } from "@/lib/cn";

interface Props {
  tone?: "cyan" | "trust" | "neutral";
  class?: string;
}

const { tone = "cyan", class: className } = Astro.props;

const tones = {
  cyan: "bg-surface-cyan text-primary-deep ring-1 ring-primary/20",
  trust: "bg-surface-cyan text-primary-deep ring-1 ring-primary/20",
  neutral: "bg-surface-slate text-ink-soft ring-1 ring-hairline",
} as const;
---

<span
  class={cn(
    "inline-flex items-center gap-1.5 rounded-pill px-3 py-1 text-sm font-medium shadow-[inset_0_1px_0_0_rgba(255,255,255,0.6)]",
    tones[tone],
    className,
  )}
>
  <slot />
</span>
